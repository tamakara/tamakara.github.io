# 技术文章编写与优化

遵循用户指定的主题和范围，将需求转化为准确、通俗、可独立阅读的教程。

## 读者与深度

- 默认读者只有基本计算机常识。按“最基础的概念 → 面试常问知识 → 日常常用操作”筛选内容，保持教程形式，不写成题库或命令清单，也不在正文反复强调读者身份。
- 解释是什么、有什么区别、什么时候用、怎么操作及如何判断结果，按主题组织，不强制每节套用同一模板。
- 必要术语首次出现时用一句通俗解释；不堆叠英文全称、缩写和需要继续查阅的新概念。
- 原理讲到能理解常见行为、选择方案和定位问题即可。规范历史、源码、内部数据结构、算法推导、报文字段、冷门变体、高级调优和复杂恢复默认不展开。
- 以“是否帮助初学者理解基础概念、回答常见问题或完成常用操作”判断内容必要性。覆盖常见选项，不百科式穷举，也不因缩短篇幅漏掉必要前提。
- 准确性靠清晰定义和必要条件保证，不靠罗列罕见例外与标准背景保证。删除数据、权限变更、版本不兼容等会影响操作正确性的限制必须保留。

## 概念、结构与独立性

- 使用标准术语并保持一致；区分协议、连接方式、物理形态、软件与具体实例，不把不同分类维度混成同类比较。
- 标题表达从属关系，层级连续；同级内容尺度相近，例如 RAID 0、RAID 1 是 RAID 的子标题。
- 用简短定义、具体例子和同维度表格解释差异。合并重复内容，避免碎片化短句、重复总结和用代码块包装普通文字。
- 每篇文章独立可读，必要背景就地简述，允许适度重复基础概念。不使用“上一篇已讲”“另文展开”等跨文章承接或预告，默认不主动串联本站文章。

## 操作与引用

- 常用示例包含前提、步骤、结果解释与验证；设备、路径、资源名一致，替换项和互斥分支明确。故障排查说明检查什么、如何判断及下一步。
- 破坏性操作前说明具体影响和停止条件，不把强制删除、关闭安全机制等当作默认修复。命令无报错不等于业务成功。
- 优先用官方资料核实关键事实，只保留与常见操作相关的版本条件。引用用于依据或可选深入阅读，不为添加引用扩充无用细节，不代替正文解释。
- 不编造来源，不把未访问或未阅读的资料声称为已核实。

## 排版与元数据

- 写作前阅读目标全文、相关 Examples 和当前配置。使用连贯段落、比较表、步骤列表及适量提示框，避免装饰堆砌。
- 支持提示框 `:::note`、`:::tip`、`:::important`、`:::warning`、`:::caution`，可加 `[标题]`；代码块注明语言，可使用 Expressive Code 标题与重点行、脚注和页内链接。
- 加粗必须成对闭合，标记内部不留首尾空格；后接正文时在结束标记后留空格，例如 `**HDD（机械硬盘）** 依靠旋转盘片存取数据。`。修改时排除代码块和行内代码。
- 当前未配置 Mermaid，不添加插件、依赖或主题功能，不默认支持 Pandoc 专属语法。
- 默认保留路径、标题、发布日期、分类、标签和发布状态；用户要求改名时同步文件名与 title，不擅自调整路由或 slug。更新摘要和修改日期，新增内容样例默认标记草稿。

## 流程与验收

1. 确认范围，阅读全文和样例，梳理标题树及必需知识。
2. 核对关键事实，重构正文，控制术语密度和深度，补齐常用操作闭环。
3. 检查独立可读性、标题归属、示例一致性、引用、页内链接、加粗、围栏、提示框和元数据；批量任务检查改名冲突及遗漏。
4. 仅修改授权内容。内容任务默认只做文本与资料检查，不运行开发服务器、构建、测试、格式化命令，不执行文章示例。
5. 交付时说明修改范围和实际检查结果，如实报告未做的渲染或运行验证。
