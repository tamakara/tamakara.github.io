# 技术文章编写与优化

本文件指导 Agent 编写和优化本站技术文章。遵循用户提供的主题、方向与范围；将口语化需求转化为准确的概念分类、结构和应用说明，不机械沿用用户措辞。

## 内容范围与深度

- 默认深度止于实际应用：让读者理解对象、选择方案、完成常用操作、验证结果并定位常见问题，不在正文刻意强调读者身份。
- 先确定文章要解决的问题，再选择必要内容。覆盖主题内常见类型和使用场景，不因某个示例只采用一种方案而遗漏其他常见选项，也不追求百科式穷举。
- 内容按“必要概念 → 使用条件与取舍 → 常用操作 → 验证与排查”组织，按主题需要选用，不强制每节套用完整模板。
- 原理只解释到足以支持选择、操作和故障判断。源码、内部数据结构、算法推导、协议字段、罕见变体和复杂恢复默认不展开，以简短说明和权威链接承接；用户明确要求时再深入。
- 用“这段内容是否改变读者的选择、操作或判断”检验必要性。无直接帮助的细节删去或转为延伸阅读；必要前提和重要限制不能为缩短篇幅而省略。

## 概念与结构

- 使用标准组织、官方文档和主流技术资料中的术语，首次出现时按需解释中文、英文与缩写，全文名称一致。
- 先区分分类维度和概念层次，再安排标题与表格。不要将协议、互连、物理形态、软件实现或具体实例当作同类对象；跨层标准应说明覆盖范围，不强行归类。
- 标题表达从属关系：同级内容尺度相近，子主题归入父主题，级别连续。例如 RAID 各级属于 RAID 的子标题，工具通常归入其服务的任务。
- 比较方案时采用一致维度，如用途、适用条件、能力与限制。统一解释深度，不让一项深入实现而另一项只有定义。
- 用准确简短的定义配合具体例子或必要示意图。类比辅助理解，不能替代定义或制造错误关系。
- 合并同篇重复主题，减少碎片化段落、重复结论和无意义的过渡语，不按篇幅机械删减。

## 独立阅读与引用

- 每篇文章尽可能独立，必要背景在本页简述，允许不同文章适度重复基础概念。
- 不使用“上一篇已介绍”“另文再讲”等跨文章承接或预告，不依赖本站其他文章的阅读顺序。默认不主动串联其他文章。
- 页内跳转用于导航；外部引用用于依据和可选深入阅读，不代替正文必需的解释。
- 关键结论优先核对标准、官方手册和发行版文档，就近链接具体资料。说明影响结论的版本、环境和假设，区分标准能力、实现行为与正式支持范围。
- 不凭印象编造来源，不将无法访问或未阅读的资料声称为已核实，不使用脱离条件的绝对性能或兼容性结论。

## 操作与表达

- 常用操作形成闭环：前提、步骤、关键输出解释、结果验证。故障排查说明症状、检查对象、判断依据和下一步，不只堆叠命令。
- 同一示例的设备、路径和资源名保持一致，标明替换项；独立场景和互斥分支明确区分，避免被误当成连续步骤。
- 修改或破坏性操作之前说明具体影响、必要条件和停止条件，不以强制选项作为通用修复。关键条件不藏在脚注或折叠内容中。
- 命令、配置和输出分开，虚构输出标注为示例。验证实际状态，不将命令无报错等同于目标完成。
- 使用连贯、通俗且准确的中文；段落解释因果，列表表达步骤或并列项，表格比较同维度对象。避免口号、空泛强调和用代码块包装普通词句。

## Markdown 与站点约定

- 写作前参考 `src/content/posts/Examples/`，并以当前站点配置确认语法能力，不能照搬样例中未经支持确认的语法。
- 按需使用表格、页内链接、脚注、主题提示框，以及 Expressive Code 的代码标题、重点行和文本标记；服务于理解，不堆砌装饰。
- 代码块标明语言；简洁关系图可用 `text`。提示框支持 `:::note`、`:::tip`、`:::important`、`:::warning`、`:::caution`，可加 `[标题]`。
- 当前未配置 Mermaid；不为文章新增插件、依赖或主题功能。不要默认 Pandoc 专属语法可用。
- 改写保留文章路径、标题、发布日期和发布状态，按需更新摘要和更新日期；用户另有要求时按其要求处理。新增内容样例默认标记 `draft: true`。

## 工作流程与验收

1. 阅读目标全文、用户约束和相关样例，确定应用范围、概念分类与标题树。
2. 核对关键事实，补齐常见选项和必要背景，重构正文，控制深度并合并重复内容。
3. 检查独立可读性、标题归属、示例一致性、操作前提、引用、页内锚点、围栏与提示框闭合及元数据。
4. 仅修改约定的内容文件，不顺带修改其他文章、主题、代码或依赖。内容任务默认只做文本与资料检查，不运行开发服务器、构建、测试或格式化命令，不执行文章中的操作示例。
5. 交付时简要说明文件和主要变更，如实报告验证范围与未确认项，不声称完成未执行的检查。
